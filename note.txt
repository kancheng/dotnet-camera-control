根據 C# 的內容，去跟電腦的攝像頭連線，判斷電腦有沒有攝像頭。

同時有錄影、拍照的功能，同時能設定

1. 按下後多久時間能拍照片。 

2. 按下後，開始錄影多久時間後錄影結束。

兩個都存在某個目錄下，該目錄會產生一個時間標籤的目錄，名稱是年月日時分秒，將當時的檔案存進去，同時要判斷建立存檔時，年月日時分秒的目錄有一致的就後面再加上 "_" + 當時的數字  EX _1 、_2 、_3

存放的目錄可以由使用者設定，除了預設，會自動抓一個 json，該 json 存放各設定，若沒有則自動建立有預設值的 json

然後相機跟錄影的可以記錄剩下幾秒，此外顯示當下時間。